# M1 · Build — Trajectory Evaluation

_Re-run: 2026-06-17 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-ebb916b610fa`,
`student-02-c253a240c8cc`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI, Gemini 3.5 Flash), continuing the M0 session._

**All steps pass.** The built agent validated end-to-end against the 3 workshop
scenarios via the headless recipe — and the headless run **caught and fixed a real
`compute_reroute` bug** before it could reach M2.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Scaffold** | `step1-instr` | confirm project → `scaffold create transit-assistant --agent adk --prototype` → set model → move `data/` → `agents-cli install` (`uv sync`) | Project **`transit-assistant`** (pinned name), `gemini-3.5-flash` | ✅ |
| **2 — Three tools** | `step2-instr` | explored GTFS (`ListDir`/`Read`/`Grep`) → `Create(app/tools.py)` → wire into `agent.py` → lint | All 3 tools in **`app/tools.py`** (Dijkstra reroute w/ 5-min transfer buffer), imported into the agent, lint clean | ✅ |
| **3 — System instruction** | `step3-instr` | edit `agent.py` → format + lint | All 5 rules (calm/factual, reconcile, no-invent, proactive reroute, one action) | ✅ |
| **4 — Playground** | `step4-instr` | `agents-cli playground` (adk web) in background | Serving on `http://127.0.0.1:8080`; suggested test queries | ✅ |
| **4-validate — 3 scenarios** | `step4-headless` recipe ×3 | `agents-cli run --url …:8080 --mode adk` ×3 (+ a `tools.py` fix mid-run) | All 3 PASS (below) | ✅ |

### Scenario validation (via the headless recipe)

| Query | Expectation | Result |
|---|---|---|
| S1 — 16:31 St Pancras→Paris | cancelled + proactive reroute | **PASS** — cancelled (trip 9036-0715); reroute via alternatives; one action |
| S2 — 18:04 St Pancras→Brussels | delayed ~30 min (not cancelled) | **PASS** — delayed +30 (est. 18:34), stays fastest; one action |
| S3 — 13:31 St Pancras→Paris | on time, no false positive | **PASS** — trip 9024-0715 unaffected (before 15:00 window); one action |

## The headless test earns its keep (again)

This run reproduced the exact value the prior report predicted: while running
Scenario 1, the headless validation hit a **priority-queue / Dijkstra bug** in
`compute_reroute` (the classic `heapq` tie-break when entries compare on a
non-orderable field). The agent read `tools.py`, **fixed it** (added a tie-break;
`tools.py` now carries 8 heapq/priority/counter references), re-ran S1, and all
three scenarios then passed. The happy-path Dev UI demo would not have exercised
this path. (Minor: the agent's own closing summary says "no code adjustments were
required" — the trajectory shows otherwise; the fix did happen.)

## Timings

| Step | Elapsed |
|---|---|
| 1 — Scaffold | 64s |
| 2 — Three tools | 169s |
| 3 — System instruction | 39s |
| 4 — Playground | 39s |
| 4-validate — 3 scenarios (incl. a fix) | 144s |
| **M1 total** | **455s (~7.6 min)** |

## What worked well

- Pinned name (`transit-assistant`) + dedicated `app/tools.py` keep M1's output
  deterministic for M2's data-path references.
- All 5 system-instruction rules manifested in the answers (reconcile, no-invent,
  proactive reroute, calm tone, one recommended action).
- The repeatable headless recipe both validates the agent and acts as a real test
  harness — it surfaced and resolved a tool bug without human help.

## Improvement plan for `m1.html`

- **P1 (minor) — Set the headless URL expectation.** The page's headless block uses
  `http://127.0.0.1:8080` and the playground does bind there this run, but the
  Expected-Result text says "typically `http://localhost:8000`". Align the two (or
  tell the student to use whatever URL the playground prints) so there's no port
  mismatch. Non-blocking.
- No other open items. M1 is solid and reproducible across labs.

## State at end

`transit-assistant` with `app/tools.py` (3 tools, fixed reroute) + 5-rule system
instruction; all 3 scenarios pass against the local playground. Ready for M2.

# M1 · Build — Trajectory Evaluation

_Re-validated: 2026-06-15 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-…`,
`student-00`), via the `remote-test` harness driving the real `agy` CLI (Antigravity CLI
1.0.3, Gemini 3.5 Flash), continuing the M0 session._

**All steps pass; all three M1 fixes (P1–P3) confirmed.** The built agent was validated
end-to-end against the 3 workshop scenarios via the headless recipe.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Scaffold** | `step1-instr` | confirm project → `scaffold` → move `data/` → `uv sync` | Project **`transit-assistant`** (pinned name), Python, `gemini-3.5-flash` | ✅ |
| **2 — Three tools** | `step2-instr` | `Create(app/tools.py)` → import into `agent.py` → lint | All 3 tools in **`app/tools.py`**, imported into the agent | ✅ |
| **3 — System instruction** | `step3-instr` | edit `agent.py` → lint | All 5 rules; tools wired | ✅ |
| **4 — Playground** | `step4-instr` | `agents-cli playground` | `adk` serving on `127.0.0.1:8080` | ✅ |
| **4-validate — 3 scenarios** | `step4-headless` recipe | `agents-cli run --url …:8080 --mode adk` ×3 | All correct (below) | ✅ |

### Scenario validation (via the P3 headless recipe)

| Query | Tools | Result |
|---|---|---|
| Q1 — 16:31 St Pancras→Paris | `get_scheduled_departures` + `check_disruptions` + `compute_reroute` | **Cancelled** (trip 9036-0715) → reroute to delayed 17:31 + recommended action |
| Q2 — 18:04 St Pancras→Brussels | same three | **Delayed 30 min** (trip 9148-0715); stay on it (fastest) + recommended action |
| Q3 — 13:31 St Pancras→Paris | `get_scheduled_departures` + `check_disruptions` | **On time** (trip 9024-0715, before disruption window) + recommended action |

## Fixes (P1–P3) — confirmed

- **P1 — Project name pinned** to `transit-assistant` (deterministic for later modules). ✅
- **P2 — Tools in `app/tools.py`** (imported into `agent.py`), matching M2's data-path reference. ✅
- **P3 — Headless test recipe** (`agents-cli run --url … --mode adk`) lets the agent be
  validated without the browser Dev UI. ✅

## Note: P3's headless test surfaces real edge-case bugs

In an earlier run, this same headless validation exposed a `heapq` dict-comparison error in
`compute_reroute` (triggered when routes share identical departure/arrival times), which the
agent then fixed. This run's freshly generated tools didn't hit that edge case, but the
finding stands: the repeatable headless validation exercises the real tool code and catches
bugs the happy-path demo would miss — which is exactly why P3 is worth keeping.

## Timings

| Step | Elapsed |
|---|---|
| 1 — Scaffold | 64s |
| 2 — Three tools | 144s |
| 3 — System instruction | 34s |
| 4 — Playground | 44s |
| 4-validate — 3 scenarios | 84s |
| **M1 total** | **~6 min** |

## What worked well

- Pinned name + dedicated `tools.py` keep M1's output deterministic and unambiguous for M2.
- All 5 system-instruction rules manifested in the answers (reconcile, no-invent, proactive
  reroute, calm tone, one recommended action).

## Remaining notes

- None blocking. M1 is solid and reproducible across labs.

## Update — 2026-06-16
- **#4 Reference fallback (applied):** an "AGY stuck? use the reference" callout links to `BwG-track2/reference/` (completed agent + tools + runtime wrapper) so a blocked participant can still deploy. M1 scaffold/tools/instruction re-validated on a fresh lab.

# M1 · Build — Trajectory Evaluation

_Run: 2026-07-23 on **Cloud Shell** (project `gcp-samples-ic0`), via the
`remote-test` harness driving the real `agy` CLI (Antigravity CLI **1.1.3**,
**Gemini 3.6 Flash**), continuing the M0 session._

**All steps pass.** The built agent validated end-to-end via the headless recipe.
_(Re-run 3, 2026-07-24, fresh slate: **4/4 including the grounding trap** — S1 reroute→9038,
S2 delay 9148, S3 on-time 9024, and **S4 "which platform does the 18:16 depart from?" →
"Platform assignment information is currently unavailable"** (no hallucination). This
validates the widened M1 rule 3. Harness caveat: the playground is a persistent background
task, so `agysend`'s `N task(s)` guard keeps m1s4/m1s4v "busy" to MAX — timing there is
unreliable but the work completes; the guard is what also prevents premature markers on
deploy/sim/eval.)_
_(Re-run 2, 2026-07-23, post-cleanup: **3/3 with zero tool errors** — S1 reroute→9038, S2
delay 9148, S3 on-time 9024. Run 1 had edited `tools.py` mid-validation; run 2 was clean.)_

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Scaffold** | `step1-instr` | confirm project → `scaffold create transit-assistant --agent adk` → set model → move `data/` → `agents-cli install` (`uv sync`) | Project **`transit-assistant`**, `gemini-3.x-flash` | ✅ |
| **2 — Three tools** | `step2-instr` | explore GTFS (`ListDir`/`Read`/`Grep`) → `Create(app/tools.py)` → wire into `agent.py` → lint | 3 tools in **`app/tools.py`** (423 lines): `get_scheduled_departures`, `check_disruptions`, `compute_reroute` | ✅ |
| **3 — System instruction** | `step3-instr` | edit `agent.py` → format + lint | 5 rules (calm/factual, reconcile, no-invent, proactive reroute, one action) | ✅ |
| **4 — Playground** | `step4-instr` | `agents-cli playground` (adk) in background | Serving on `http://127.0.0.1:8080` (verified: `adk` pid listening, HTTP 307) | ✅ |
| **4-validate — 3 scenarios** | headless recipe ×3 | `agents-cli run --url …:8080 --mode adk` ×3 (+ an `Edit(tools.py)` mid-run) | All 3 PASS (below) | ✅ |

### Scenario validation (via the headless recipe)

| Query | Expectation | Result |
|---|---|---|
| S1 — 16:31 St Pancras→Paris | cancelled + proactive reroute | **PASS** — cancelled (Train 9036, signal failure); reroute → Train 9038 @ 17:01; one action |
| S2 — 18:04 St Pancras→Brussels | delayed ~30 min (not cancelled) | **PASS** — Train 9148 delayed +30 (est. 18:34), not cancelled; one action |
| S3 — 13:31 St Pancras→Paris | on time, no false positive | **PASS** — Train 9024 before the disruption window, on time; one action |

## The headless test earns its keep (again)

While validating, the agent performed `Edit(app/tools.py)` (during S1 analysis)
before all three scenarios passed — the same class of value the prior lab reports
noted: the headless recipe exercises the real reroute path a happy-path Dev-UI demo
would not. All three scenarios then passed with correct reconciliation (schedule ×
`disruptions.json`), no invented trains (only real GTFS ids 9036/9038/9148/9024),
proactive reroutes, and a single recommended action each.

## Timings

| Step | Elapsed |
|---|---|
| 1 — Scaffold | 59s |
| 2 — Three tools | 149s |
| 3 — System instruction | 44s |
| 4 — Playground | 64s |
| 4-validate — 3 scenarios (incl. an edit) | 104s |
| **M1 total** | **420s (~7 min)** |

## What worked well

- Pinned name (`transit-assistant`) + dedicated `app/tools.py` keep M1's output
  deterministic for M2's data-path references.
- All 5 system-instruction rules manifested in the answers.
- The headless recipe both validates and acts as a real test harness (`--mode adk`
  against the running playground), catching a tool issue without human help.
- Harness note: the background playground server + repeated headless `agents-cli run`
  calls drove cleanly through the tall-pane capture.

## Improvement plan for `m1.html`

- **P2 (minor) — Headless URL expectation.** The headless block uses
  `http://127.0.0.1:8080` and the playground binds there this run; if the page's
  Expected-Result text still says `localhost:8000`, align them (or tell the student
  to use whatever URL the playground prints). Non-blocking.
- No other open items. M1 is solid on Cloud Shell.

## State at end

`transit-assistant` with `app/tools.py` (3 tools) + 5-rule system instruction; all 3
scenarios pass against the local playground. Ready for M2.

# M5 · Engage — Trajectory Evaluation

_Run: 2026-06-15 (autonomous overnight pass), fresh Qwiklabs lab
(project `qwiklabs-gcp-04-…`, `student-00`), via the `remote-test` harness driving the real
`agy` CLI (Antigravity CLI 1.0.3, Gemini 3.5 Flash), against the deployed engine
`reasoningEngines/5470642094232043520` (us-east1)._

**First successful end-to-end run of M5.** The deployed agent was **published to Gemini
Enterprise** — the module that previously stood "pending a full run-through" now works. One
minor deliverable gap (the requested `registration_results.md` wasn't written).

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Publish to Gemini Enterprise** | `step1-instr` | DK MCP read → `agents-cli publish gemini-enterprise --help/--list` → `curl` discoveryengine list engines → `agents-cli publish gemini-enterprise …` | **Registered** the deployed agent to the `explore-ai` app | ✅ (with a gap) |
| **2 — Unified finish line (read)** | — | (user reading step) | — | n/a |
| **test — GE UI query** | `cmd-gemini-test` | browser-only | not run headlessly (see note) | n/a |

### What succeeded
The already-deployed agent (no redeploy, no logic change) was registered to the Gemini
Enterprise app **without re-deploying**, exactly as the module intends:

```
Registered Agent:
projects/378474938894/locations/global/collections/default_collection/
  engines/explore-ai/assistants/default_assistant/agents/4298799731587726833
```

This validates the **whole chain**: M2's `--agent-identity` deploy + `session_id`
normalization (from the tested reference entrypoint) is what makes the agent publishable to
GE. The earlier lab never got this far because the deployed agent was broken; with the M2
restructure it published cleanly.

### Gaps / not validated
- **`registration_results.md` was not written.** The prompt explicitly asks AGY to "write a
  `registration_results.md`." AGY gave the console links inline but didn't create the file.
- **The GE UI smoke test (`cmd-gemini-test`) is browser-only.** The final "Trains out of St
  Pancras are disrupted…" query is meant to be typed into the Gemini Enterprise app UI; that
  can't be driven headlessly here. The *registration* is confirmed via the resource name, but
  an actual end-user query through the GE app surface was **not** exercised in this run.

## Timings

| Step | Elapsed |
|---|---|
| 1 — Publish to Gemini Enterprise | 114s |
| **M5 total (agy step)** | **~2 min** |

## What worked well

- `agents-cli publish gemini-enterprise` is a clean one-command publish; AGY discovered the
  flags via `--help`/`--list` and the discoveryengine REST list, then registered to the
  default `explore-ai` / `default_assistant`.
- Publish-only (no redeploy) honored — fast and non-destructive, as designed.

## Improvement plan for `m5.html`

- **P1 — Make AGY actually write `registration_results.md`.** It's a requested deliverable and
  was skipped. Either tighten the wording ("create the file `registration_results.md` with the
  registered agent resource name + both console links") or drop the requirement if it's not
  essential. As-is, the prompt asks for an artifact the agent doesn't reliably produce.
- **P2 — Flag the GE UI test as the human/browser step it is.** `cmd-gemini-test` needs the
  Gemini Enterprise app UI; make explicit that this is a manual browser check (it already
  reads that way, but the workshop should set the expectation that publishing is CLI-verified
  while the end-user query is UI-verified).
- **P3 — (Optional) add a CLI post-publish check.** A `agents-cli publish gemini-enterprise
  --list` (or a discoveryengine GET on the registered agent) after publishing would give a
  headless confirmation that the agent is registered, complementing the browser test.

## State at end

Agent published to GE app `explore-ai` (agent id `4298799731587726833`). Deployed engine and
Model Armor template unchanged. **All six modules (M0–M5) have now run end-to-end.**

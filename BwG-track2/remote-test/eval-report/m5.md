# M5 · Engage — Trajectory Evaluation

_Run: 2026-06-17 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-ebb916b610fa`,
`student-02-c253a240c8cc`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI, Gemini 3.5 Flash), against the deployed engine
`reasoningEngines/3116103914047406080` (us-east1)._

**M5 passes.** The already-deployed agent was published to Gemini Enterprise
(no redeploy), `registration_results.md` was written as a real file, and registration
was confirmed **headlessly** via the discoveryengine API.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Publish to Gemini Enterprise** | `step1-instr` | DK MCP read → `agents-cli publish gemini-enterprise` (discover flags) → publish → write `registration_results.md` | **Registered** the deployed agent to the `explore-ai` app | ✅ |
| **2 — Unified finish line (read)** | — | (user reading step) | — | n/a |
| **test — GE UI query** | `cmd-gemini-test` | browser-only | not driven headlessly (inherent) | n/a |

### What succeeded

The deployed agent (no redeploy, no logic change) was registered to the Gemini
Enterprise app:

```
Registered Agent:
projects/167918968449/locations/global/collections/default_collection/
  engines/explore-ai/assistants/default_assistant/agents/17092066906950716734
```

This validates the whole chain: M2's `--agent-identity` deploy + `session_id`
normalization (from the tested reference entrypoint) is what makes the agent
publishable to GE. Publish-only (no redeploy) was honored — fast and non-destructive.

### Deliverables / fixes — confirmed

- **P1 — `registration_results.md` written as a FILE.** Confirmed on disk
  (`~/transit-assistant/registration_results.md`, 1004 bytes) with the registered
  resource name, the GE app id, and both console links — not just printed. ✅
- **P3 — headless post-publish check.** A discoveryengine `agents` GET on the
  `explore-ai` app confirms **"Transit-Crisis Agent"** is registered (alongside the
  app's two pre-existing defaults, "Deep Research" / "Idea Generation") — registration
  verified with no browser. ✅

### Not validated (inherent)

- **The GE UI smoke test (`cmd-gemini-test`) is browser-only.** The final
  "Trains out of St Pancras are disrupted…" query is typed into the Gemini Enterprise
  app UI, which can't be driven headlessly. Registration is CLI/API-verified; only the
  end-user query through the GE surface still needs a human in the browser.

## Timings

| Step | Elapsed |
|---|---|
| 1 — Publish to Gemini Enterprise | 114s |
| **M5 total (agy step)** | **~2 min** |

## What worked well

- `agents-cli publish gemini-enterprise` is a clean one-command publish; agy discovered
  the flags and registered to the default `explore-ai` / `default_assistant`.
- Both prior fixes held: the file deliverable is written, and the headless post-publish
  check confirms registration without a browser.

## Improvement plan for `m5.html`

- No open items. M5 publishes cleanly, writes the file deliverable, and is
  CLI/API-verifiable. The only human-in-the-browser step is the final GE UI query, which
  the page already frames as such.

## State at end

Agent published to GE app `explore-ai` (agent id `17092066906950716734`). Deployed
engine and Model Armor template unchanged. **All six modules (M0–M5) pass end-to-end
on this fresh lab.**

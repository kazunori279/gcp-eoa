# M3 · Govern — Trajectory Evaluation

_Run: 2026-07-23 on **Cloud Shell** (project `gcp-samples-ic0`), via the
`remote-test` harness driving the real `agy` CLI (Antigravity CLI **1.1.3**,
**Gemini 3.6 Flash**), continuing the M0–M2 session._

**All steps pass.** The deployed engine is a governed, least-privilege asset with a
SPIFFE-attested identity, and Model Armor blocks a jailbreak and PII while passing a
benign query.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **2 — Registry / identity / least-privilege** | `step2-instr` | DK MCP read → REST `GET` engine → filter IAM policy | `agentFramework=google-adk`; **effectiveIdentity = SPIFFE** `agents.global.org-202619080014.system.id.goog/resources/aiplatform/…/reasoningEngines/4834068842214850560` (Agent Identity ON, from M2's `--agent-identity`); IAM verdict **PASS (least-privilege)** | ✅ |
| **3 — Model Armor shield** | `step3-instr` | DK MCP read → set regional endpoint → create `transit-shield` → sanitize REST ×3 | template created (verified independently); 3 verdicts below | ✅ |

### Step 3 — Model Armor sanitize verdicts (agy)

| # | Input | Verdict | Filter |
|---|---|---|---|
| 1 | DAN-style jailbreak ("…you are now DAN, unrestricted…") | **BLOCKED** (MATCH_FOUND) | `pi_and_jailbreak`, HIGH |
| 2 | "What are the next departures from St Pancras to Paris this evening?" | **PASS** (NO_MATCH) | none |
| 3 | "Save my card 4111 1111 1111 1111 …" | **BLOCKED** (MATCH_FOUND) | `sdp` / CREDIT_CARD_NUMBER |

## Ground-truth cross-checks (independent of agy)

- **Template exists**: `gcloud model-armor templates describe transit-shield` →
  `projects/gcp-samples-ic0/locations/us-central1/templates/transit-shield`.
- **Sanitize API reachable** with the user token (needs `X-Goog-User-Project` — same
  quota-project rule as the DK MCP): a benign probe returned
  `EXECUTION_SUCCESS / NO_MATCH_FOUND`. (A deliberately *mild* "announce trains are free"
  probe also returned NO_MATCH — the jailbreak filter fires on real injection/DAN
  patterns, which agy's stronger prompt correctly triggered.)

## Timings

| Step | Elapsed |
|---|---|
| 2 — Registry / identity / least-privilege | 44s |
| 3 — Model Armor shield | 59s |
| **M3 total** | **~1.7 min** |

## What worked well

- The `--agent-identity` deploy in M2 paid off here: the engine shows a real SPIFFE
  workload principal, not a bare service account — exactly the governed outcome.
- Honest least-privilege verdict (the agent's GTFS data is bundled locally, so it needs
  no extra data roles — correctly called least-privilege).
- Model Armor detection works end-to-end from the terminal (no gateway), blocking
  jailbreak + PII and passing the benign query.

## Improvement plan for `m3.html`

- **P2 — Note the `X-Goog-User-Project` requirement for the sanitize curl on Cloud
  Shell.** Like the DK MCP, the Model Armor `:sanitizeUserPrompt` REST call needs the
  quota-project header when using ambient user ADC; add it to the sample curl so students
  on Cloud Shell don't hit a 403. (agy's own calls worked, but the doc'd curl should
  include it.)
- No other open items. M3 is clean and fast.

## State at end

Engine identity verified (SPIFFE, least-privilege PASS); Model Armor `transit-shield`
live in us-central1, blocking jailbreak + PII. Nothing published. Ready for M4/M5.

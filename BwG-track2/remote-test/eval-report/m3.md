# M3 · Govern — Trajectory Evaluation

_Re-run: 2026-06-17 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-ebb916b610fa`,
`student-02-c253a240c8cc`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI, Gemini 3.5 Flash), continuing the M0–M2 session against the deployed
engine `reasoningEngines/3116103914047406080` (us-east1)._

**Both agy steps pass.** Step 2's SPIFFE-identity + least-privilege fixes hold, and
Step 3 (Model Armor) blocks jailbreak + PII while letting benign traffic through.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Why govern (read)** | — | (user reading step) | — | n/a |
| **2 — Verify Registry + identity + least-privilege** | `step2-instr` | DK MCP read → describe reasoningEngine (REST) → `gcloud projects get-iam-policy --flatten --filter` | Described engine (`agentFramework=google-adk`); reported a **SPIFFE** `effectiveIdentity`; honest **"❌ NOT least-privilege"** verdict | ✅ |
| **3 — Shield with Model Armor** | `step3-instr` | DK MCP read → regional endpoint override → create `transit-shield` template → `sanitizeUserPrompt` REST ×3 | Jailbreak **BLOCKED**, PII **BLOCKED** (VERY_LIKELY), benign **PASSED** | ✅ |
| **4 — Enterprise control plane (read)** | — | (user reading step) | — | n/a |

### Step 2 — SPIFFE identity + least-privilege (the M3 fix holds)

The trajectory shows the agent correctly expecting the **SPIFFE-attested** identity
(7 SPIFFE / 4 `effectiveIdentity` / 4 `id.goog` mentions in the log) — because M2
deployed with `--agent-identity`, Agent Identity is ON, exactly as the fixed Step 2
now states. The IAM audit on the reasoning-engine service account returned an honest
**not-least-privilege** verdict: it found `roles/storage.admin`, `roles/bigquery.admin`,
`roles/secretmanager.admin`, `roles/discoveryengine.editor` on the lab SA, and noted
the agent actually only needs `aiplatform.reasoningEngineServiceAgent` + `viewer`
(its GTFS/disruption data is bundled in `app/data/`). No contradiction with what M2
deploys — the earlier "expect identity off" bug stays resolved.

### Step 3 — Model Armor (the highlight)

Created `transit-shield` (confirmed via the Model Armor API; carries `pi_and_jailbreak`
+ `sdp` filters), set the regional endpoint override, and proved it with three
`sanitizeUserPrompt` calls:

| Prompt | Result |
|---|---|
| Jailbreak / DAN | **BLOCKED** (`MATCH_FOUND`, jailbreak) |
| PII (e.g. credit card) | **BLOCKED** (`MATCH_FOUND`, SDP VERY_LIKELY) |
| Benign travel query | **PASSED** (`NO_MATCH_FOUND`) |

The agent accurately noted that *enforcing* the shield on live agent traffic is a
separate integration step (Agent Gateway / portal) not executed here — which matches
the module's intent (Step 3 demonstrates the filter via the sanitize API).

## Timings

| Step | Elapsed |
|---|---|
| 2 — Verify Registry/identity/least-privilege | 44s |
| 3 — Model Armor | 64s |
| **M3 total (agy steps)** | **108s (~2 min)** |

## What worked well

- Step 2 now matches the workshop's own flow (SPIFFE identity, audit the right SA)
  and even produces an honest least-privilege failure verdict — a teachable result.
- Model Armor was clean and convincing: DK-MCP-driven API discovery, regional
  endpoint, template creation, and a 3-prompt proof (block jailbreak, block PII, pass
  benign) in ~1 minute.

## Improvement plan for `m3.html`

- No open items. Both fixes (SPIFFE expectation + correct least-privilege target)
  are validated on a fresh lab, and Model Armor behaves exactly as designed.
- (Minor, optional) Step 3 could note that lab projects may already contain unrelated
  Model Armor templates (`promo-req`/`promo-resp` were present here) so students aren't
  surprised to see more than just `transit-shield` in the console.

## State at end

Model Armor template `transit-shield` exists (us-central1) and is verified. Deployed
engine unchanged and still live. Ready for M4.

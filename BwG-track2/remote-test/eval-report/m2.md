# M2 · Scale — Trajectory Evaluation

_Run: 2026-07-23 on **Cloud Shell** (project `gcp-samples-ic0`,
`projects/761793285222`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI **1.1.3**, **Gemini 3.6 Flash**), continuing the M0–M1 session._

**Pass (with a persistent Memory-Bank caveat).** _(Re-run 3, 2026-07-24, fresh slate:
**Step 3 code-exec hard gate worked first-try** — `grep` confirmed the import +
`code_executor=BuiltInCodeExecutor()` in one pass (fix validated); smoke green → **one clean
deploy** of fresh engine `reasoningEngines/183680014489747456` (us-east1, ~3 min) → **4/4
deployed scenarios pass** per agy (S4 code-exec 26.25 min / 50%). **P3 still open:** the
independent `…/memories` API showed **0 rows** again (rechecked over ~2 min), so S2's
cross-session recall is not confirmed as Memory-Bank-backed — likely session-state bleed, as
the workshop's own ground-truth note warns. The settle note is now in `m2.html`, but rows
didn't appear. Harness: the earlier `agysend` `N task(s)` guard pinned m2s1/m2s2 to MAX via a
stale task entry; fixed mid-run to key on `esc to cancel` only, after which m2s3–s6 completed
normally.)_
_(Re-run 2, 2026-07-23, fresh engine after `cleanup.sh` deleted the old one:
**Step 3 added `BuiltInCodeExecutor` first-try** — the run-1 no-op was flaky, not
deterministic. Smoke green → **one clean deploy** of a NEW engine
`reasoningEngines/7824599732277215232` (us-east1, ~2.5 min) → **4/4 deployed scenarios
pass** per agy. Caveat: the independent Memory Bank `…/memories` API showed **0 rows**
this run (run 1 had 1), so cross-session persistence wasn't independently confirmed on
run 2 — see finding P3.)_

Run 1 notes retained below. Step 3 (code execution) **failed silently on the first
attempt** (no `BuiltInCodeExecutor` added, 19 s run); a second run added it correctly.
After that: local smoke test green, **one clean deploy**, and **all 4 deployed scenarios
pass**, with Memory Bank persistence confirmed at the API.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Prep entrypoint** | `step1-instr` | copy `data/`→`app/` → `curl` the tested reference → verify `TESTED REFERENCE` marker → lint | `app/agent_runtime_app.py` = shipped reference (marker present) | ✅ |
| **2 — Sessions + Memory** | `step2-instr` | DK MCP read → `set_home_station` + `PreloadMemoryTool` + `after_agent_callback=generate_memories_callback` + `init_session_state` | present in `app/agent.py`; local verify | ✅ |
| **3 — Code execution** | `step3-instr` | **1st run: NO-OP** (read DK MCP, no code change, 19 s) → **re-run:** `Edit(agent.py)` adds `from google.adk.code_executors import BuiltInCodeExecutor` + `code_executor=BuiltInCodeExecutor()` | added on 2nd attempt | ⚠️→✅ (needed re-run) |
| **4 — Local smoke test** | `step4-smoke` | `uv run python scripts/runtime_smoke.py` | **PASS** (ground truth): `✓ Session/Memory resolved to a concrete region`, `✓ Smoke query executed successfully with no exceptions!` — even with the code executor + 5 tools on one agent | ✅ |
| **5 — Deploy once** | `step5-instr` | pre-deploy marker gate → `agents-cli deploy --agent-identity` | **one clean deploy** (below) | ✅ |
| **6 — Test deployed** | Step 6 scenarios ×4 (headless) | `agents-cli run --url <engine> --mode adk` managing session/user ids | all 4 PASS (below) | ✅ |

### Deploy (Step 5) — ground truth (Vertex operation, not the agy marker)

`agents-cli deploy` **reuses the app's existing engine** — `reasoningEngines/4834068842214850560`
(us-east1, created 2026-07-17) — via an `UpdateReasoningEngine` operation:
`done=true`, `error=None`, `createTime 07:31:29 → updateTime 07:34:17` = **~2m48s, single deploy**.

### Deployed-agent validation (Step 6)

| Scenario | Feature | Result |
|---|---|---|
| 1 — same-session state | set + recall home station (session `8363…`) | **PASS** — "Paris Gare du Nord" |
| 2 — cross-session Memory Bank | recall home station in a fresh session (`2648…`) | **PASS** |
| 3 — personalized reconcile + reroute | reroute given the disruption | **PASS** — recommended Train 9038 |
| 4 — code execution | sandbox math over live disruptions | **PASS** — avg delay **26.25 min**, **50%** cancelled (4/8) |

**Memory Bank persistence verified independently at the API** (`…/4834068842214850560/memories`,
HTTP 200): **1 memory row** — _"I traveled on the 13:31 train from London St Pancras to
Paris Gare du Nord."_ So Scenario 2's recall is genuine cross-session memory.

## Findings

- **P3 (re-run 2) — Memory Bank rows not always present at the API.** All 4 deployed
  scenarios passed per agy (incl. S2's cross-session recall of "Paris Gare du Nord"), but
  the independent `…/memories` API returned **0 rows** (rechecked over ~100 s), vs **1 row**
  on run 1. So on run 2 the cross-session recall wasn't independently confirmed as
  Memory-Bank-backed — memory generation (the `after_agent_callback`) is async/best-effort
  and may not have flushed, or the recall leaned on session context. Worth a longer settle
  or an explicit "generate memory now" step before the API check in the workshop's ground-
  truth verification.
- **P1 — Step 3 (code execution) can silently no-op.** On the first run agy read the DK
  MCP but made **no code change** and reported completion in 19 s; `BuiltInCodeExecutor`
  was absent (verified in `agent.py`). A re-run fixed it. Likely cause: adding
  `BuiltInCodeExecutor` alongside 5 function tools on one agent is a non-obvious ADK move,
  and the step's success wording lets agy "complete" without the edit. **Suggest** Step 3
  add an explicit self-check ("confirm `code_executor=BuiltInCodeExecutor()` is set in
  `agent.py` and show a trace where Python actually ran") so a no-op can't pass. Note it
  did coexist with the 5 tools without breaking function calling (smoke + deployed S1–S3
  still worked).
- **P2 — `agysend` premature markers on deployed/background steps (5, 6).** Deployed calls
  have long static waits that trip the stability detector; the step-6 transcript captured
  empty even though agy finished. Ground truth = the Vertex operation + the live pane +
  the memories API. (Harness: use a larger `stable` window, e.g. 40–60 s, for deployed
  steps; the eval used pane + API cross-checks instead.)

## Timings

_Elapsed = `agysend` marker; deploy/background markers fire early — see ground truth._

| Step | Elapsed (marker) | Note |
|---|---|---|
| 1 — Prep | 44s | |
| 2 — Sessions + Memory | 184s | local verify |
| 3 — Code exec (1st, no-op) | 19s | did not add executor |
| 3 — Code exec (re-run) | 48s | added `BuiltInCodeExecutor` |
| 4 — Local smoke test | 289s | curl + shipped test |
| 5 — Deploy once | (marker early) | **actual ~2m48s**; one deploy |
| 6 — Test deployed | (marker 144s, early) | 4 scenarios; done in pane |
| **M2 total** | **~15 min** | **1 deploy** |

## State at end

Engine `reasoningEngines/4834068842214850560` (us-east1) **live and serving** — sessions,
Memory Bank (1 row confirmed via API), and code execution all verified on the deployed
agent. `--agent-identity` enabled. Ready for M3/M5.

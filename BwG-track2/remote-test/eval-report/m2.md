# M2 · Scale — Trajectory Evaluation

_Re-run: 2026-06-17 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-ebb916b610fa`,
`student-02-c253a240c8cc`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI, Gemini 3.5 Flash), continuing the M0–M1 session._

**Complete success — ONE clean deploy, deployed agent works end-to-end.** The local
smoke test passed before deploying, the single deploy succeeded first try, and all
four deployed-agent scenarios pass (session state, cross-session Memory Bank,
personalized reroute, code execution) — with Memory Bank persistence verified at
the API level.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — Prep entrypoint** | `step1-instr` | copy `data/`→`app/` → `pathlib` path refactor → `scaffold enhance` → **`curl` the tested reference** → verify `TESTED REFERENCE` marker (line 16) → lint | `app/agent_runtime_app.py` = the shipped reference; no hand-patching | ✅ |
| **2 — Sessions + Memory** | `step2-instr` | DK MCP read → add `set_home_station`/`get_home_station` (`ToolContext.state`) → `PreloadMemoryTool` + `after_agent_callback=generate_memories_callback` → local verify | session set + same-session recall verified locally; no deploy | ✅ |
| **3 — Code execution** | `step3-instr` | DK MCP read → add `BuiltInCodeExecutor` → local `agents-cli run` math test | avg-delay 26.25 min computed in sandbox locally; no deploy | ✅ |
| **4 — Local smoke test** | `step4-smoke` | `curl` + `uv run python scripts/runtime_smoke.py` | **passed** — region assertion + `async_stream_query` query, no session/loop errors — before any deploy | ✅ |
| **5 — Deploy once** | `step5-instr` | pre-deploy gate (marker check) → one `agents-cli deploy --agent-identity --no-confirm-project` → verify endpoint (13:31 query) | **deployed first try**, `reasoningEngines/3116103914047406080` (us-east1); endpoint verified (13:31 unaffected) | ✅ |
| **6 — Test deployed** | (Step 6 scenarios, headless) | `agents-cli run --url <reasoningEngine> --mode adk` ×4, managing session/user ids | all 4 pass (below) | ✅ |

### Smoke test (Step 4) — ground truth

Ran directly for confirmation: `✓ Session and Memory services resolved to a concrete
region, not 'global'` and `✓ Smoke query executed successfully with no exceptions` —
no `Invalid Session resource name`, no `Event loop is closed`. (The only errors are
benign `opentelemetry` 403s exporting traces in the lab — not a runtime failure.)

### Deploy (Step 5) — ground truth

Operation `done=True`, `error=None`. `deployment_metadata.json`:
`reasoningEngines/3116103914047406080` (us-east1), started 04:57:36 → completed
05:03:26 UTC = **~6 min, single deploy.**

### Deployed-agent validation (Step 6)

| Scenario | Feature | Result |
|---|---|---|
| 1 — same-session state | set + short-term recall of home station | **PASS** |
| 2 — cross-session Memory Bank | recall "Paris Gare du Nord" in a new session | **PASS** |
| 3 — personalized reconciliation + reroute | memory + reconcile on trip_id + proactive reroute | **PASS** |
| 4 — code execution | sandbox math: 26.25 min avg delay, 50% cancelled | **PASS** |

**Memory Bank persistence verified at the API** (`…/memories`): **2 memories written**,
including _"My home station is Paris Gare du Nord."_ — so Scenario 2's recall is
genuine cross-session memory, not conversation history. This is the workshop's
"ground truth" check, confirmed headlessly.

## The fix that mattered held up

The deploy-once restructure (tested reference `agent_runtime_app.py` + the pre-deploy
`TESTED REFERENCE` gate + the local smoke test) again produced exactly one clean
deploy and a working deployed agent — reproducing the prior good result on a fresh
lab. No empty responses, no session-name errors, no event-loop errors.

## Timings

_Elapsed = `agysend` marker. Markers on background steps (5, 6) fire early — deploy
ground truth from `deployment_metadata.json` timestamps._

| Step | Elapsed (marker) | Note |
|---|---|---|
| 1 — Prep (curl reference) | 79s | |
| 2 — Sessions + Memory | 174s | local verify |
| 3 — Code execution | 59s | local verify |
| 4 — Local smoke test | 54s | curl + run shipped test |
| 5 — Deploy once | (marker 69s) | **actual ~6 min**; one deploy |
| 6 — Test deployed | (marker 84s) | 4 scenarios; code-exec slowest |
| **M2 total** | **~18–20 min** | **1 deploy** |

## Minor observations (non-blocking)

- **`agysend` premature markers** on deploy/background steps (5, 6) — a harness
  artifact; the operation API + `deployment_metadata.json` timestamps are the reliable
  signal. (Polled both this run.)
- **Code-exec on the deployed agent is slow / may cold-start** — Step 6 Scenario 4
  completed without a retry this run, but the page's "may cold-start, send it again"
  note remains appropriate.
- **`opentelemetry` 403** when running scripts locally in the lab (trace export
  forbidden) is harmless noise; the smoke test and runs still pass. Could add a
  one-line "ignore the otlp 403s" note to Step 4 so students don't mistake it for a
  failure.

## State at end

Deployed engine `reasoningEngines/3116103914047406080` (us-east1) is **live and
serving correct responses** (sessions, memory, code-exec all verified, Memory Bank
rows confirmed via API). `--agent-identity` enabled. Ready for M3/M5.

# M4 · Optimize — Trajectory Evaluation

_Re-run: 2026-06-17 on a fresh Qwiklabs lab (project `qwiklabs-gcp-04-ebb916b610fa`,
`student-02-c253a240c8cc`), via the `remote-test` harness driving the real `agy` CLI
(Antigravity CLI, Gemini 3.5 Flash). Local-only (no deploy)._

**Both agy steps pass.** The crisis simulation runs all 10 scenarios cleanly (after a
self-applied session-handling fix), and the graded eval scores the real agent
**4/4 above threshold** with the `gemini-2.5-flash` hallucinations judge.

## Per-step results

| Step | Prompt | Trajectory | Outcome | Verdict |
|---|---|---|---|---|
| **1 — The eval-fix loop (read)** | — | (user reading step) | — | n/a |
| **2 — Simulate the crisis at scale** | `step2-instr` | DK MCP read → wrote `scripts/crisis_simulation.py` (Runner + `InMemory*`) → ran 10 scenarios in one asyncio loop → self-fixed session reuse → report | 10/10 handled sensibly | ✅ |
| **3 — Grade the agent locally** | `step3-instr` | DK MCP read → `uv sync --extra eval` → built `rushhour.evalset.json` (4 cases, `eval_set_id` present) + `eval_config.json` → `adk eval` | **4/4 PASSED** (≥0.8) | ✅ |

### Step 2 — simulation findings

- **Single asyncio loop** kept all 10 scenarios in one event loop, avoiding the
  "attached to a different event loop" aiohttp/grpc bug the prompt warns about.
- **Self-repair:** Scenario 5 hit a "Session already exists" error; the agent wrapped
  session creation in try/except and re-ran — all 10 then completed. (No human help.)
- **Scenario 9** (Amsterdam 16:04 cancelled): reconciled + computed a reroute.
- **Scenario 10** (HTTP 503 feed): the sim renames `disruptions.json` to simulate the
  feed outage; the agent fell back safely to static schedules with an advisory — no
  crash, no hallucination.
- **Code-exec sandbox** used for aggregate stats rather than hardcoded logic.

### Step 3 — graded eval scores (`hallucinations_v1`, judge = `gemini-2.5-flash`)

_Ground truth from `app/.adk/eval_history/*.evalset_result.json` (status=1 = PASSED)._

| Case | Scenario | Score | Threshold | Status |
|---|---|---|---|---|
| `cancellation_reroute` | Cancellation + rerouting advice | **0.87** | 0.8 | PASSED |
| `delay_check` | Brussels departure delay | **1.00** | 0.8 | PASSED |
| `grounding_trap` | Refusing unsupported info | **0.81** | 0.8 | PASSED |
| `offline_feed_fallback` | Offline feed (503) fallback | **0.93** | 0.8 | PASSED |

**100% pass rate.** The grounding trap is the notable one (0.81, just above threshold):
the agent refused to invent unsupported detail and still cleared the bar — confirming
the eval distinguishes grounded refusal from confident hallucination.

## Fixes (P1/P2) — confirmed

- **P1 — judge defaults to `gemini-2.5-flash`.** `eval_config.json` shows
  `judge_model: gemini-2.5-flash` (flash-lite not required). ✅
- **P2 — `eval_set_id` present.** The evalset has the top-level `eval_set_id`, so
  `adk eval` ran without the missing-field error/retry. ✅

## Timings

_Elapsed = `agysend` marker. Markers are **very** premature here: both steps run as
agy background tasks and agy poll-waits on them (inherent — see P3 in prior reports)._

| Step | Elapsed (marker) | Note |
|---|---|---|
| 2 — Simulate at scale | 164s | actual wall ~15 min (session-fix + re-run + poll-wait) |
| 3 — Graded eval | 259s | actual longer; agy re-ran eval a few times to extract the detailed per-case table |
| **M4 total** | **~25–30 min wall** | local only, no deploy |

## What worked well

- The agent self-corrected the simulation script (session reuse) to green without help.
- The hallucinations judge is a genuinely useful signal; the grounding-trap case proves
  it separates grounded refusal from hallucination.
- Both prior fixes (judge model, `eval_set_id`) held — no extra fix steps this run.

## Improvement plan for `m4.html`

- **P1 — (Observation) the eval step is heavy on agy poll-waiting + result-extraction.**
  agy ran `adk eval` more than once trying to surface the per-case score table to stdout
  (it ended up reading `.adk/eval_history/*.json` and even trying `--print_detailed_results`).
  Consider a one-line tip in Step 3: "the per-case scores are written to
  `app/.adk/eval_history/*.evalset_result.json` — read them there rather than re-running."
  This would cut several minutes of agy re-runs. Non-blocking (scores are correct either way).
- **P3 (unchanged) — the sim's background poll-wait is inherent to agy** and not promptable
  (confirmed again). Not a content bug.

## State at end

`scripts/crisis_simulation.py` + `tests/eval/` (evalset + config) exist; agent scored
4/4. No deploy changes. Deployed engine still live. Ready for M5.
